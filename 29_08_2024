
  Calculate the area of a circle given its radius.

  
  function circle(radius) {
  let result = 3.14 * radius * radius;
  return result;
   }

let area = circle(5);
console.log(area);


Implement a program to find the largest of three numbers.

let a=[2,5,9];
let b=0;



for(i=0;i<a.length;i++)

if(a[i]>b){
  b=a[i]
}


console.log(b +" is the greatest number")




Write a program to check if a person is eligible to vote based on their age and citizenship.


let age=prompt("what is your age ?")


let citizenship=prompt("Did you have the Citizneship?")



if((age>=18) && (citizenship===true)){
  
alert("the voter is eligibility to vote")
}
else{
  alert("the voter is not eligibility to vote")
}






Calculate the square root of a number.


function squares(number) {

  const squareOfNumber = number * number;
  return squareOfNumber;
}
let result = squares(5);
console.log(result);




Convert a JavaScript object into a JSON string.

const data = {
  name: "John Jerish",
  age: 27,
  email: "johnjerish@gmail.com",
};
const result = JSON.stringify(data)
console.log(result);




Add, remove, or modify properties in a JSON object.

let person = {
  name: "john",
  age: 27,
  city: "Nagercoil"
};

person.email = "johnjerishj@gmail.com";
person.age = 26;
delete person.city;

console.log(person);


Implement a function to reverse an array.


const numbers = [2, 4, 6, 1,9];


for (let i = 0; i < numbers.length; i++) {
  for (let j = 0; j < numbers.length - 1 - i; j++) {
    if (numbers[j] > numbers[j + 1]) {

      const temp = numbers[j];
      numbers[j] = numbers[j + 1];
      numbers[j + 1] = temp;
    }
  }
}

console.log(numbers);


